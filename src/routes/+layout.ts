export const prerender = true;

/**
 * Every URL on this site ends in a slash, as it did under Eleventy, so that the legacy links,
 * redirects and page-relative asset references in the content all keep resolving.
 */
export const trailingSlash = 'always';
